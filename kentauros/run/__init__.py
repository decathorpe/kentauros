"""
This subpackage contains the ``run`` and ``run_config`` functions, which are
called when the package is executed by the setuptools-installed ``ktr`` and
``ktr-config`` scripts as entry points.
"""
